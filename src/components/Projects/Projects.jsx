import React from 'react'
import Header from './Header'
import Section from './Section'

const Projects = () => {
  return (
    <div id='projects' className={`wrapper d-flex flex-column `}>
        <Header />
        <Section />
    </div>
  )
}

export default Projects